import React from "react";

const Header: React.FC = () => {
  return (
    <header>
      <div className="pr-32">
        <button>Header Button</button>
      </div>
    </header>
  );
};

export default Header;
